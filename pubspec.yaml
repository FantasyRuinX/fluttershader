name: ShaderRenderer
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  flutter_shaders: ^0.1.3
  cupertino_icons: ^1.0.8
  flutter_animate:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  shaders:
    - shaders/ShaderWorld_0.frag
    - shaders/ShaderWorld_1.frag

